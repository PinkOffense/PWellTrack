# ── Database ──
# Local dev (SQLite):
DATABASE_URL=sqlite+aiosqlite:///./pwelltrack.db
# Production (Supabase Postgres):
# DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

# ── Auth ──
SECRET_KEY=change-me-in-production-use-a-real-secret
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ── Supabase (for Google OAuth) ──
SUPABASE_URL=https://bbrlzpxctxwqclwudnuj.supabase.co
SUPABASE_ANON_KEY=

# ── CORS ──
# Comma-separated origins. Use * for dev, restrict in production.
CORS_ORIGINS=*
# Production example:
# CORS_ORIGINS=https://pwelltrack.vercel.app,https://your-domain.com
